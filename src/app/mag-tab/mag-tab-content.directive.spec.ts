import { MagTabContentDirective } from './mag-tab-content.directive';

describe('MagTabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new MagTabContentDirective();
    expect(directive).toBeTruthy();
  });
});
