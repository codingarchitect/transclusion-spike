import { MagTabDirective } from './mag-tab.directive';

describe('MagTabDirective', () => {
  it('should create an instance', () => {
    const directive = new MagTabDirective();
    expect(directive).toBeTruthy();
  });
});
